# LAYA AI Service - Environment Configuration
# Copy this file to .env and update with your values

# ============================================================================
# Database Configuration
# ============================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=laya_ai
POSTGRES_USER=laya
POSTGRES_PASSWORD=laya_password

# ============================================================================
# JWT Authentication Configuration
# ============================================================================
# CRITICAL: This secret MUST match the JWT_SECRET_KEY in Gibbon's configuration
# to enable cross-service authentication. Generate a secure random secret using:
#   ./scripts/generate-jwt-secret.sh
# or:
#   python -c "import secrets; print(secrets.token_urlsafe(64))"
#
# NEVER commit the actual secret to version control!
# NEVER use the default value in production!
JWT_SECRET_KEY=your_jwt_secret_key_change_in_production
JWT_ALGORITHM=HS256

# ============================================================================
# Security Notes
# ============================================================================
# 1. The JWT_SECRET_KEY must be:
#    - At least 32 characters long (64+ recommended)
#    - Cryptographically random
#    - Kept secret and secure
#    - The SAME in both AI Service and Gibbon configurations
#
# 2. Both services use this shared secret to:
#    - Sign JWT tokens (Gibbon auth_token.php endpoint)
#    - Verify JWT tokens (AI Service middleware)
#
# 3. If you change this secret:
#    - All existing JWT tokens will become invalid
#    - Users will need to re-authenticate
#    - Update it in BOTH services simultaneously
#
# 4. Store this secret securely:
#    - Use environment variables (Docker secrets, K8s secrets)
#    - Never hardcode in source code
#    - Rotate periodically (every 90 days recommended)
