# LAYA AI Service Environment Configuration
# Copy this file to .env and update with your values

# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=laya_ai
POSTGRES_USER=laya
POSTGRES_PASSWORD=laya_password

# JWT Configuration
JWT_SECRET_KEY=your_jwt_secret_key_change_in_production
JWT_ALGORITHM=HS256

# CORS Configuration - Security Lockdown
# Comma-separated list of allowed origins for production
# Leave empty for development defaults (localhost:3000, localhost:8080, localhost:8000)
# Production example: CORS_ORIGINS=https://app.layaedu.com,https://parent.layaedu.com
CORS_ORIGINS=
