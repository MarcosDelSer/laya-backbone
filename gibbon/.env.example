# Gibbon - Environment Configuration for JWT Authentication
# This file should be loaded by your PHP environment or Docker container

# ============================================================================
# JWT Authentication Configuration
# ============================================================================
# CRITICAL: This secret MUST match the JWT_SECRET_KEY in AI Service's configuration
# to enable cross-service authentication. Generate a secure random secret using:
#   ./scripts/generate-jwt-secret.sh
# or:
#   openssl rand -base64 64
#
# NEVER commit the actual secret to version control!
# NEVER use the default value in production!
JWT_SECRET_KEY=your_jwt_secret_key_change_in_production

# ============================================================================
# How to Use This File
# ============================================================================
# For Docker deployments:
#   Add to docker-compose.yml under environment or env_file
#
# For Apache/Nginx with PHP-FPM:
#   Add to php-fpm pool configuration or use SetEnv in Apache config
#
# For testing/development:
#   Source this file: source .env (after copying to .env)
#
# Example Docker Compose:
#   services:
#     php-fpm:
#       environment:
#         - JWT_SECRET_KEY=${JWT_SECRET_KEY}
#       env_file:
#         - .env

# ============================================================================
# Security Notes
# ============================================================================
# 1. The JWT_SECRET_KEY must be:
#    - At least 32 characters long (64+ recommended)
#    - Cryptographically random
#    - Kept secret and secure
#    - The SAME in both Gibbon and AI Service configurations
#
# 2. This secret is used by:
#    - /modules/System/auth_token.php (to sign JWT tokens)
#
# 3. The AI Service verifies tokens using the same secret
#
# 4. If you change this secret:
#    - All existing JWT tokens will become invalid
#    - Users will need to re-authenticate
#    - Update it in BOTH services simultaneously
#
# 5. Store this secret securely:
#    - Use environment variables (Docker secrets, K8s secrets)
#    - Never hardcode in source code
#    - Rotate periodically (every 90 days recommended)
