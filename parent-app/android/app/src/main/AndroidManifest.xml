<?xml version="1.0" encoding="utf-8"?>
<!--
  LAYA Parent App - Android Manifest

  This manifest declares the app's permissions, components, and configuration.
  Equivalent to the iOS Info.plist for permission descriptions.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ==================== PERMISSIONS ==================== -->

    <!-- Internet access for API communication and content delivery -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Network state for checking connectivity status -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Camera permission for capturing photos -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Storage permissions for accessing and saving photos -->
    <!-- For Android 13+ (API 33+), use granular media permissions -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- For Android 10-12, use scoped storage with legacy option -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- Push notification permission for Android 13+ (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Vibrate permission for notification feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Wake lock for keeping the device awake during important operations -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Receive boot completed to reschedule notifications after device restart -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Foreground service permission for background operations -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- ==================== HARDWARE FEATURES ==================== -->

    <!-- Camera feature - not required, app can work without camera -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <!-- ==================== APPLICATION ==================== -->

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:theme="@style/AppTheme"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true">

        <!-- Main Activity - Single entry point for React Native -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <!-- Main launcher intent filter -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep linking support for laya-parent:// scheme -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="laya-parent" />
            </intent-filter>
        </activity>

        <!-- ==================== FIREBASE CLOUD MESSAGING ==================== -->

        <!-- Firebase Messaging Service for handling push notifications -->
        <service
            android:name="com.google.firebase.messaging.FirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Default notification channel for FCM -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="laya_parent_notifications" />

        <!-- Default notification icon -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />

        <!-- Default notification color -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />

        <!-- ==================== BOOT RECEIVER ==================== -->

        <!-- Receive boot completed broadcasts to reschedule notifications -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
